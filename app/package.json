{
  "name": "app",
  "version": "1.0.0",
  "description": "Example application for using dependencies between containers with \"healthcheck (Docker)\"",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "tsc": "tsc",
    "dev": "nodemon --watch './' --exec 'ts-node ./server.ts' -e ts"
  },
  "keywords": [
    "Docker",
    "TypeScript",
    "Containers",
    "MySQL"
  ],
  "author": "Lucian Tavares",
  "license": "ISC",
  "dependencies": {
    "@types/express": "^4.17.18",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "mysql2": "^3.6.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "ts-node": "^10.9.1"
  }
}
